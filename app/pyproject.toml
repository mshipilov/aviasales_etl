[project]
name = "app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "selenium>=4.41.0",
    "setuptools>=82.0.1",
    "undetected-selenium>=1.1",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
